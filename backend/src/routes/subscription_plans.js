
const express = require('express');

const Subscription_plansService = require('../services/subscription_plans');
const Subscription_plansDBApi = require('../db/api/subscription_plans');
const wrapAsync = require('../helpers').wrapAsync;

const router = express.Router();

const { parse } = require('json2csv');
/**
 *  @swagger
 *  components:
 *    schemas:
 *      Subscription_plans:
 *        type: object
 *        properties:

 *          name:
 *            type: string
 *            default: name

 *          trial_period:
 *            type: integer
 *            format: int64

 */

/**
 *  @swagger
 * tags:
 *   name: Subscription_plans
 *   description: The Subscription_plans managing API
 */

  /**
  *  @swagger
  *  /api/subscription_plans:
  *    post:
  *      security:
  *        - bearerAuth: []
  *      tags: [Subscription_plans]
  *      summary: Add new item
  *      description: Add new item
  *      requestBody:
  *        required: true
  *        content:
  *          application/json:
  *            schema:
  *              properties:
  *                data:
  *                  description: Data of the updated item
  *                  type: object
  *                  $ref: "#/components/schemas/Subscription_plans"
  *      responses:
  *        200:
  *          description: The item was successfully added
  *          content:
  *            application/json:
  *              schema:
  *                $ref: "#/components/schemas/Subscription_plans"
  *        401:
  *          $ref: "#/components/responses/UnauthorizedError"
  *        405:
  *          description: Invalid input data
  *        500:
  *          description: Some server error
  */

router.post('/', async (req, res) => {
    await Subscription_plansService.create(req.body.data, req.currentUser, true, req.headers.referer);
    const payload = true;
    res.status(200).send(payload);
});

  /**
  *  @swagger
  *  /api/subscription_plans/{id}:
  *    put:
  *      security:
  *        - bearerAuth: []
  *      tags: [Subscription_plans]
  *      summary: Update the data of the selected item
  *      description: Update the data of the selected item
  *      parameters:
  *        - in: path
  *          name: id
  *          description: Item ID to update
  *          required: true
  *          schema:
  *            type: string
  *      requestBody:
  *        description: Set new item data
  *        required: true
  *        content:
  *          application/json:
  *            schema:
  *              properties:
  *                id:
  *                  description: ID of the updated item
  *                  type: string
  *                data:
  *                  description: Data of the updated item
  *                  type: object
  *                  $ref: "#/components/schemas/Subscription_plans"
  *              required:
  *                - id
  *      responses:
  *        200:
  *          description: The item data was successfully updated
  *          content:
  *            application/json:
  *              schema:
  *                $ref: "#/components/schemas/Subscription_plans"
  *        400:
  *          description: Invalid ID supplied
  *        401:
  *          $ref: "#/components/responses/UnauthorizedError"
  *        404:
  *          description: Item not found
  *        500:
  *          description: Some server error
  */

router.put('/:id', wrapAsync(async (req, res) => {
  await Subscription_plansService.update(req.body.data, req.body.id, req.currentUser);
  const payload = true;
  res.status(200).send(payload);
}));

  /**
  * @swagger
  *  /api/subscription_plans/{id}:
  *    delete:
  *      security:
  *        - bearerAuth: []
  *      tags: [Subscription_plans]
  *      summary: Delete the selected item
  *      description: Delete the selected item
  *      parameters:
  *        - in: path
  *          name: id
  *          description: Item ID to delete
  *          required: true
  *          schema:
  *            type: string
  *      responses:
  *        200:
  *          description: The item was successfully deleted
  *          content:
  *            application/json:
  *              schema:
  *                $ref: "#/components/schemas/Subscription_plans"
  *        400:
  *          description: Invalid ID supplied
  *        401:
  *          $ref: "#/components/responses/UnauthorizedError"
  *        404:
  *          description: Item not found
  *        500:
  *          description: Some server error
  */

router.delete('/:id', wrapAsync(async (req, res) => {
  await Subscription_plansService.remove(req.params.id, req.currentUser);
  const payload = true;
  res.status(200).send(payload);
}));

  /**
  *  @swagger
  *  /api/subscription_plans:
  *    get:
  *      security:
  *        - bearerAuth: []
  *      tags: [Subscription_plans]
  *      summary: Get all subscription_plans
  *      description: Get all subscription_plans
  *      responses:
  *        200:
  *          description: Subscription_plans list successfully received
  *          content:
  *            application/json:
  *              schema:
  *                type: array
  *                items:
  *                  $ref: "#/components/schemas/Subscription_plans"
  *        401:
  *          $ref: "#/components/responses/UnauthorizedError"
  *        404:
  *          description: Data not found
  *        500:
  *          description: Some server error
  */

router.get('/', wrapAsync(async (req, res) => {
  const filetype = req.query.filetype
  const payload = await Subscription_plansDBApi.findAll(
    req.query,
  );
  if (filetype && filetype === 'csv') {
    const fields = ['id','name',
        'trial_period',

        ];
    const opts = { fields };
    try {
      const csv = parse(payload.rows, opts);
      res.status(200).attachment(csv);
      res.send(csv)

    } catch (err) {
      console.error(err);
    }
  } else {
    res.status(200).send(payload);
  }

}));

/**
 *  @swagger
 *  /api/subscription_plans/count:
 *    get:
 *      security:
 *        - bearerAuth: []
 *      tags: [Subscription_plans]
 *      summary: Count all subscription_plans
 *      description: Count all subscription_plans
 *      responses:
 *        200:
 *          description: Subscription_plans count successfully received
 *          content:
 *            application/json:
 *              schema:
 *                type: array
 *                items:
 *                  $ref: "#/components/schemas/Subscription_plans"
 *        401:
 *          $ref: "#/components/responses/UnauthorizedError"
 *        404:
 *          description: Data not found
 *        500:
 *          description: Some server error
 */
router.get('/count', wrapAsync(async (req, res) => {
    const payload = await Subscription_plansDBApi.findAll(
        req.query,
        { countOnly: true }
    );

    res.status(200).send(payload);
}));

/**
 *  @swagger
 *  /api/subscription_plans/autocomplete:
 *    get:
 *      security:
 *        - bearerAuth: []
 *      tags: [Subscription_plans]
 *      summary: Find all subscription_plans that match search criteria
 *      description: Find all subscription_plans that match search criteria
 *      responses:
 *        200:
 *          description: Subscription_plans list successfully received
 *          content:
 *            application/json:
 *              schema:
 *                type: array
 *                items:
 *                  $ref: "#/components/schemas/Subscription_plans"
 *        401:
 *          $ref: "#/components/responses/UnauthorizedError"
 *        404:
 *          description: Data not found
 *        500:
 *          description: Some server error
 */
router.get('/autocomplete', async (req, res) => {
  const payload = await Subscription_plansDBApi.findAllAutocomplete(
    req.query.query,
    req.query.limit,
  );

  res.status(200).send(payload);
});

  /**
  * @swagger
  *  /api/subscription_plans/{id}:
  *    get:
  *      security:
  *        - bearerAuth: []
  *      tags: [Subscription_plans]
  *      summary: Get selected item
  *      description: Get selected item
  *      parameters:
  *        - in: path
  *          name: id
  *          description: ID of item to get
  *          required: true
  *          schema:
  *            type: string
  *      responses:
  *        200:
  *          description: Selected item successfully received
  *          content:
  *            application/json:
  *              schema:
  *                $ref: "#/components/schemas/Subscription_plans"
  *        400:
  *          description: Invalid ID supplied
  *        401:
  *          $ref: "#/components/responses/UnauthorizedError"
  *        404:
  *          description: Item not found
  *        500:
  *          description: Some server error
  */

router.get('/:id', wrapAsync(async (req, res) => {
  const payload = await Subscription_plansDBApi.findBy(
    { id: req.params.id },
  );

  res.status(200).send(payload);
}));

router.use('/', require('../helpers').commonErrorHandler);

module.exports = router;
